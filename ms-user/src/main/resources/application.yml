spring:
  application:
    name: ms-user
  datasource:
    url: jdbc:mysql://localhost:3307/db-user
    username: root
    password: AulaMatriz123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
springdoc:
  api-docs:
    path: /api-docs
  show-actuator: false
  packages-to-scan: com.aulamatriz.ms.management.controller
control:
  my-exception: "nuevo mensaje"